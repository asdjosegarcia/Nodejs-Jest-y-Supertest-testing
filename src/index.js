import app from "./app.js";//importamos el modulo app.js, o que nos da la respuesta que esta en app.js

app.listen(3000)//the server will run on port 3000
console.log('Server is running on port 3000');